# vigour

Vigour is Fortran implementation of the VimScript specification separate from the `vim` application itself. It is intended to become a VimScript to C compiler.


Currently, it is only a tokeniser
